# Tru-T source and infrastructure

## To use Tru-T software directly

Head to the `application/` directory and look at the README there.

## To setup the API

Head to the `terraform/` directory and look at the README there.